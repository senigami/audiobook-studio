# Task 001 — Delete 5 dead CSS selectors

DONE — 2026-07-10 (commit `52be0584`, per `../status.json`). The 5 dead selectors (`.btn-home`,
`.btn-menu-destructive`, `.action-menu-item`, `.select-glass`, `.engine-chunk`) were removed from
`theme/components.css` before the split; no JSX referenced them. See the ST-1–ST-4 summary in
`design-docs/specs/design-system.md` changelog row 1.14.0.
