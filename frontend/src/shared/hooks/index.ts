// Shared hook boundary for Studio 2.0.
//
// Shared hooks should stay small and generic so feature logic does not migrate
// into the shared layer accidentally.

export const sharedHooksPlaceholder = true;
