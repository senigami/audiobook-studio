// Shared component boundary for Studio 2.0.
//
// Shared UI should live here only when it is truly cross-feature and not a
// disguised feature-specific component.

export const sharedComponentsPlaceholder = true;
