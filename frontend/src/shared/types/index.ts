// Shared type boundary for Studio 2.0.
//
// Prefer API contracts and feature-local types first; only move a type here
// when it is truly shared and not domain-specific state in disguise.

export type SharedTypePlaceholder = true;
