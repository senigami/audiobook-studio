// Query boundary for Studio 2.0.
//
// Canonical entity loading belongs here and in feature-level query helpers, not
// in the live overlay stores.

export const createStudioQueries = () => {
  throw new Error('Studio 2.0 query helpers are not implemented yet.');
};
