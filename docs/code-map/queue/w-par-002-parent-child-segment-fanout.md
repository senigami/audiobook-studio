2026-07-02 — W-PAR 002: added `app/orchestration/tasks/segment_synthesis.py` (`SegmentSynthesisTask` ephemeral child, `ChapterSynthesisTask` durable parent fanning chunk groups from `build_chunk_groups` into a bounded `ThreadPoolExecutor`, admitted per-child under task 001's engine-class semaphores via `_manifest_resource_claim`); `app/orchestration/tasks/synthesis.py` unchanged in behavior (only lost the appended classes, re-imported by the new module — still 416 lines, same `SynthesisTask`). Structural-only: `orchestrator.py`'s `submit()`, `plugins/tts_mixed/handler.py`, `recovery.py`, and `progress/service.py` were deliberately left untouched (recovery already dedupes by `chapter_id`; progress `publish()` already gates at ≥1%; live-engine routing through the parent/child model is deferred to task 003, which owns the per-segment `_dispatch` state isolation this needs first). New tests: `tests/orchestration/test_parent_child_scheduling.py` (fan-out count, cap=1 serial/INV-1, cap=2 concurrent, progress aggregation with ≥1% gating, one-job-per-chapter/INV-4, cancel-stops-all-children/INV-7).
