2026-07-03 — W-PAR 005: correctness invariants under parallelism (INV-2, INV-3, INV-7, INV-8, R-C, retry-once, stuck-segment heartbeat + 004 residual). Scoped to the code that is actually live/actually concurrent today: `plugins/tts_mixed/handler.py` (today's sequential mixed render loop) for the artifact-validation gate, and `app/orchestration/tasks/segment_synthesis.py`'s `ChapterSynthesisTask`/`SegmentSynthesisTask` (002's concurrent fan-out model, not yet wired into `orchestrator.submit()`/`recover()` — that live-wiring is task 008's job per 003's own scoping notes and the map's "enabling cap>1" out-of-scope line) for the stitch barrier, cancel-join, K-of-N recovery filter hook, retry-once, and heartbeat.

**INV-3 (artifact validation), `plugins/tts_mixed/handler.py`:** added `_validated_wav_duration_seconds` (stdlib `wave`, no ffprobe/subprocess) and `_is_valid_segment_artifact` (exists + non-zero size; when parseable as a WAV, header duration must be `0 < d <= MAX_SEGMENT_DURATION_SECONDS = 3600.0`; falls back to the pre-existing exists+non-empty check for non-WAV test doubles rather than failing every stub in the existing suite closed). `_group_needs_render` now routes through `_is_valid_segment_artifact` instead of `.exists()`. The `rc != 0 or not seg_out.exists()` done-gate in `handle_mixed_job` now reads `rc != 0 or not _is_valid_segment_artifact(seg_out)` — never marks a segment done on exit code alone. INV-2 stitch order: the sequential handler's final stitch list (`fresh_groups` rebuilt via `get_chapter_segments`, `ORDER BY segment_order ASC`) was already DB-order, not completion-order, because it re-queries the DB fresh after the whole render loop finishes rather than accumulating paths during the loop — documented with an explicit comment; no functional change needed there.

**`app/orchestration/tasks/segment_synthesis.py` (002's concurrent model):**
- `TaskResult` (`app/orchestration/tasks/base.py`) gained an additive `output_path: str | None = None` field so a completed child can report its validated artifact.
- `SegmentSynthesisTask` gained `segment_order` (manuscript-order key, from the group leader segment's DB `segment_order`), `attempt` (1 or 2), `last_heartbeat`/`stalled` (touched on admission and completion).
- `ChapterSynthesisTask._fan_out_chapter` accepts an injected `needs_render_fn` — when set (the recovery path, once 008 wires it), a group is excluded from the fan-out entirely when the predicate returns False (INV-8: K-of-N, only unfinished segments resubmitted; already-valid artifacts reused as-is).
- `ChapterSynthesisTask.run()`: INV-7's `concurrent.futures.wait(futures, ALL_COMPLETED)` barrier now runs explicitly before any future's result is consumed (was previously implicit via the `ThreadPoolExecutor` context-manager exit only) — makes the join a first-class, testable step. INV-2 stitch barrier: `stitch_entries` collects `(segment_order, output_path)` per completed child and, once all are joined, sorts by `segment_order` before invoking the optional injected `self._stitch_fn` exactly once — never completion order. Retry-once (owner directive 2026-07-03): `_run_child_with_retry` runs a child; on any non-cancelled failure with `attempt < MAX_SEGMENT_ATTEMPTS(=2)`, constructs and runs a fresh replacement child with `attempt+1` once; a second failure is recorded in `self.permanently_failed_segment_ids` and does not block siblings — the chapter still resolves (as `failed`, since a permanently-failed segment means at least one group never validated) once every other child has finished; stitch is skipped whenever `had_failure` is true. Stuck-segment heartbeat: `_start_heartbeat_monitor` starts a 1s-poll daemon thread that flags `child.stalled = True` once `time.monotonic() - child.last_heartbeat > SEGMENT_STALL_TIMEOUT_SECONDS (default 60s)`; `ChapterSynthesisTask.stalled_segments` (property) surfaces the flagged child ids for a future chapter-payload wire-up (deferred to 008 — no `ProgressService.publish`/`_publish` kwarg exists yet for `stalled_segments`, unlike the already-wired `active_segments_map`). Flag-only — cancel remains the sole kill surface.

**004 residual, `plugins/tts_xtts/plugin/core/warm_worker.py` `_acquire_worker`:** the previously-bare, permanently-blocking `self._free_q.get()` fallback is now a poll loop (`self._free_q.get(timeout=_ACQUIRE_POLL_INTERVAL_SECONDS=0.5)`); each timeout, re-checks under `self._lock` whether every pooled worker has died (`not any(w.is_alive for w in self._pool)`) and, if so, clears the pool and returns `None` (one-shot fallback) instead of hanging forever. This lives inside `_acquire_worker` itself — no ad-hoc timeout was added at the `run_job` call site, per the explicit in-code directive.

**R-C (write contention):** `app/db/core.py` `get_connection()`/`get_studio_connection()` now issue `PRAGMA journal_mode=WAL` on every connect (idempotent after the first). Audited `app/db/segments.py::update_segments_bulk`/`update_segments_status_bulk` — both already route per-segment writes to the SQLite `chapter_segments` table under the existing `_db_lock`; `state.json` (`app/db/state_jobs.py`) is untouched by these paths (chapter-level `Job` writes only), confirmed by a concurrency regression test.

**Tests (`tests/orchestration/test_correctness_invariants.py`, 10 tests, R1-checked by stashing `plugins/tts_xtts/plugin/core/warm_worker.py`, `plugins/tts_mixed/handler.py`, `app/orchestration/tasks/base.py` — `segment_synthesis.py` is new/untracked so its pre-fix state is "does not exist yet", which is itself the red state for every test exercising it):**
- A `TestStitchBarrierOrder` — 2 segments complete in reverse manuscript order; stitch callback receives manuscript-ordered paths. Red pre-fix: `TaskResult.__init__() got an unexpected keyword argument 'output_path'`.
- B `TestArtifactValidatedCompletion` — zero-byte WAV rejected by `_is_valid_segment_artifact`/`_group_needs_render`; valid sibling WAVs are not flagged for rerender. Red pre-fix: `_group_needs_render` only checked `.exists()`.
- C `TestCancelJoinsAllInFlight` — cancel mid-fan-out blocks until every child observes the stop signal; no post-cancel writes. Green even pre-fix (002 already joined via the executor context manager) — kept as an explicit regression pin for the now-explicit `futures_wait` barrier.
- D `TestRecoveryKofN` — `needs_render_fn` injected; fan-out constructs children for exactly the 2 unfinished (of 4) segments. Green pre-fix in the sense that `_fan_out_chapter` existed, but the `needs_render_fn` parameter itself did not — `TypeError: unexpected keyword argument` pre-fix.
- E `TestWriteContention` — 4 concurrent `update_segments_bulk` writers; 4 correct SQLite rows; zero `state.json` `save_state` calls. Passes on both pre- and post-fix code (pins existing-good behavior; this task's job here was auditing + adding the WAL pragma, not changing the write path).
- F `TestRetryOncePolicy` (2 tests) — fails-once-then-succeeds completes the chapter with exactly 2 attempts; fails-twice is marked permanent with exactly 2 attempts (never 3), siblings unaffected, stitch skipped. Red pre-fix: no retry/permanent-failure bookkeeping existed at all.
- Heartbeat (2 tests) — stalled flag appears in `stalled_segments` past the (monkeypatched, 0.05s) threshold and the fan-out still completes; `_acquire_worker` returns (does not hang) when every pooled worker is dead. Red pre-fix: `AttributeError`/hang respectively.

**Deferred to 008 (global pull queue) — documented, not silently dropped:**
- Wiring `ChapterSynthesisTask`/`SegmentSynthesisTask` into `TaskOrchestrator.submit()`/`recover()`/`cancel()` (`orchestrator.py:386`/`:240`) — today's live dispatch path is still the sequential `handle_mixed_job` / `SynthesisTask.from_task_context` reconstruction; 002/003/005's parent/child model is fully built and unit-tested but stands alone until 008 wires the enable-gate.
- `stalled_segments` on the live chapter-level progress payload — the property exists on `ChapterSynthesisTask` and is unit-tested, but `ProgressService.publish`/`orchestrator_publish._publish` have no `stalled_segments` kwarg yet (unlike `active_segments_map`, which 003 already threaded end-to-end); adding that wire is 008's job once the parent/child model is live.
- The full per-child `SEGMENT_SAVED` emission rework in `app/api/ws.py` (still transition-inferred, correct only at today's N=1) — already flagged by 003, reconfirmed still deferred here since fan-out>1 is not live.

Verification: `tests/orchestration tests/db tests/api` → 1098 passed, 1 skipped. `plugins/tts_mixed plugins/tts_xtts tests/orchestration/test_correctness_invariants.py` → 211 passed, 1 skipped. Full suite → 2030 passed, 3 skipped (baseline 2020 + 10 new tests, zero regressions). `ruff check app/ tests/ plugins/` → clean.
