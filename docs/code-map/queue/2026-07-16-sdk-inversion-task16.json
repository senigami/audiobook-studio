{
  "at": "2026-07-17T02:00:00Z",
  "commit": "uncommitted",
  "task": "010 task 16 — E2E install-flow + trust-warning tests (plan-05 §5.3) with local git fixture (Phase E)",
  "files": {
    "app/tts_server/plugin_staging.py": {
      "change": "modified",
      "contract_changed": false,
      "summary": "Pure refactor: extracted _clone_and_stage(git_url, plugins_dir) as the post-validation body of preview_github_repo; preview_github_repo is now _clone_and_stage(_normalize_github_repo_url(git_url), plugins_dir). Test seam only — URL validation is untouched and still rejects everything except https://github.com/<owner>/<repo>."
    },
    "tests/tts_server/test_install_flow_e2e.py": {
      "change": "added",
      "contract_changed": false,
      "summary": "E2E install-flow tests over a LOCAL bare git repo fixture (tmp_path, no network): preview stages under .preview_* without installing (§5.3 'no code loaded until consent'), confirm installs into plugins_dir, cancel sweeps staging, symlink repos rejected, invalid manifests rejected. URL-validation hardening tests (R1 security property: file://, http://, non-github, credentials, query, ssh, bare path all 400). Trust-level tests: registry entries all official + repo_url matches manifest distribution.git_url; unknown community URL has no registry entry."
    },
    "design-docs/specs/install-distribution.md": {
      "change": "modified",
      "contract_changed": false,
      "summary": "spec_version 1.2.0 -> 1.3.0: new 'GitHub install/trust flow (contract)' section documenting URL validation, two-phase preview/confirm staging, symlink/manifest rejection, and the registry-membership trust model. Documentation of existing behavior; zero behavior change."
    }
  },
  "flow_impact": "None at runtime — preview_github_repo behavior byte-identical (validated URL -> clone -> validate -> stage). New tests pin the install/trust flow offline.",
  "notes": "§5.3 gap (reported, not silently implemented): the backend preview response carries no trust_level/consent flag; official-vs-community classification is client-side registry lookup and the consent dialog is frontend PluginTrustModal. Plan invariant is zero behavior change, so tests pin the existing model; the §5.3 UI acceptance (Community badge + consent dialog naming the repo) remains a frontend-level check not covered by the host pytest suite."
}
