{
  "date": "2026-07-11",
  "task": "north_star_screen_parity/tasks/009-decision-backups-tab-fate.md (Option B, Step 2b)",
  "summary": "Relocated the working ProjectBackupsPanel wiring from PublishStage.tsx to BackupsStage.tsx so the standalone Backups book-level tab has real functionality (matching the demo's tab structure); PublishStage.tsx is now assembly-only.",
  "files_changed": [
    {
      "path": "frontend/src/pages/Book/stages/BackupsStage.tsx",
      "change": "Replaced Phase-1 placeholder stub with the real ProjectBackupsPanel, wired to project/actions from useBookDataContext() (same hook/provider instance PublishStage.tsx uses, so projectId scoping is identical)."
    },
    {
      "path": "frontend/src/pages/Book/stages/PublishStage.tsx",
      "change": "Removed the ProjectBackupsPanel block and its now-unused import; stage is now assembly-only (AssemblyProgress/AssemblyPanel/AssemblyChapterPicker)."
    },
    {
      "path": "frontend/tests/unit/pages/Book/BackupsStage.test.tsx",
      "change": "New test asserting the real backups panel renders (not the Phase-2 stub) and is scoped to the current project's id."
    },
    {
      "path": "frontend/tests/unit/pages/Book/PublishStage.test.tsx",
      "change": "Updated assertion to confirm backups UI is no longer rendered in Publish."
    }
  ],
  "notes": "No routing changes needed (Option B keeps the /book/:id/backups route, just changes what it renders). Legacy frontend/src/pages/ProjectDetail/ProjectDetailPage.tsx (the original working reference implementation) is untouched — its retirement is tracked separately as R6-T10 dead-code retirement."
}
