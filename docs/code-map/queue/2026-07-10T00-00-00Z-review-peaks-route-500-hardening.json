{
  "at": "2026-07-10T00:00:00Z",
  "commit": "uncommitted",
  "task": "Adversarial review fix (Concurrency Saboteur): harden the compute-on-miss peaks route against a 500 on concurrent WAV deletion/replacement, per the plan's explicit 'degrade to 404, never 500' contract.",
  "files": {
    "app/api/routers/chapters_assets.py": {
      "change": "modified",
      "contract_changed": false,
      "summary": "_load_or_compute_peaks_sidecar: wrapped the top-level wav_path.stat() in try/except OSError (returns None when a concurrent re-render deletes the WAV between the route's exists() check and the stat -> route 404 instead of unguarded 500). Wrapped the sidecar tmp write + os.replace in try/except OSError: caching is now best-effort — a failed write logs a warning and still serves the freshly computed peaks rather than 500/discard.",
      "calls_added": [],
      "calls_removed": []
    }
  }
}
