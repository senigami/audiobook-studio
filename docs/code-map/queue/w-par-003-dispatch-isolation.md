2026-07-03 — W-PAR 003: extracted per-segment dispatch isolation (option a, keystone/INV-6) in `app/orchestration/scheduler/orchestrator_helpers.py` — `_dispatch(self, *, task, context) -> TaskResult` is now a 2-line thin fan-out driver delegating to the new `_dispatch_segment(self, *, task, context) -> SegmentResult`, which owns the full per-call isolated closure scope (`timing`, `segment_starts`, `segment_announced`, `segment_load_observed`, `marker_state`, `pending_engine_activity`, `load_state`/W-MIX-LA proactive+reactive blocks, active-segment cells, `log_listener` and every nested helper) — isolated by Python closure scope, not by keying a shared dict on `segment_id`. Every `return` site inside the old `_dispatch` body now routes through a local `_finish(result) -> SegmentResult` closure so callers always get the isolated state regardless of dispatch path (registry handler / local execution / bridge). New `SegmentResult` dataclass (`task_result`, `timing`, `marker_state`, `segment_load_observed`, `segment_starts`, `segment_announced`). At today's N=1 fan-out this is byte-identical to the pre-003 single-`_dispatch` path (INV-1, pinned via an ordered-event-sequence golden test in `tests/orchestration/test_dispatch_isolation.py`).

Added the additive `active_segments_map` field (C2 contract) end-to-end on the backend: `app/db/models.py` (`Job.active_segments_map`, optional), `app/orchestration/scheduler/orchestrator_publish.py` (`_publish(active_segments_map=...)` kwarg → `update_job(...)` updates dict, only written when non-None), `app/api/contracts/events.py` (`build_queue_item_status_event(active_segments_map=...)`, snake_case payload key matching the 006 frontend adapter's expectation verbatim), `app/api/ws.py` (`_emit_queue_item_status_frame` reads `merged.get("active_segments_map")` and passes it through). `_dispatch_segment` publishes a single-entry map (today's one active segment) at each of its `_publish` call sites: LOADING_MODEL/preparing (both the `ENGINE_ACTIVITY_STARTED` and `MODEL_LOAD_STARTED` branches), SEGMENT_PENDING/preparing, START_SEGMENT+START_SYNTHESIS/rendering, SEGMENT_PROGRESS/rendering, SEGMENT_SAVED/done.

Scope decision (directed by the coordinator's phase-2 review): wiring fan-out > 1 into the live dispatch path (`orchestrator.submit()`, `plugins/tts_mixed/handler.py`) is explicitly OUT of scope for 003 — deferred to task 005/enable-gate. Consequently `app/api/ws.py`'s transition-inferred `SEGMENT_SAVED` emission (`prev_active_segment_id != new_active_segment_id`) was left unchanged — it is already correct at N=1 (one active segment per chapter job means prev→next IS the only handoff) — with its failed/cancelled status-preserving mapping now pinned by a new regression test (`test_broadcast_job_updated_segment_completion_failed_status_preserved`, R1-checked). The full per-child-completion emission rework remains 005/enable-gate's to do once concurrent children are live.

Specs bumped same commit: `design-docs/specs/live-events.md` → 1.9.0 (`active_segments_map` on `queue_item_status`, additive/optional, snake_case wire key); `design-docs/specs/queue-jobs.md` → 1.8.0, new §3.10 (dispatch shape, parent/child model current scope, `active_segments_map` emission, unchanged `segments.progress` transition emission + deferred rework).

New tests: `tests/orchestration/test_dispatch_isolation.py` (3 tests — two-concurrent-segment isolation cross-talk via `_dispatch_segment` with `threading.Event`-gated interleaving and a thread-local watchdog dispatch table to avoid a `mock.patch` cross-thread race; `active_segments_map` C2 shape from a two-group `_dispatch` call; pinned cap=1 ordered-event-sequence golden baseline). One new test in `tests/api/test_websocket_broadcast.py` for the failed-status preservation above. One pre-existing test (`test_progress_contract_v140.py::TestB8DiagnosticLogging`) updated to inspect `_dispatch_segment`'s source instead of `_dispatch`'s (the closure's new home; no behavior change).

Verification: `tests/orchestration/ tests/api/` → 877 passed, 1 skipped. Full suite → 2020 passed, 3 skipped (baseline 2016 + 4 new tests). `ruff check app/ tests/` → clean.
