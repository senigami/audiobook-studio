{
  "at": "2026-07-16T23:45:00Z",
  "commit": "uncommitted",
  "task": "010 task 13 — manifest distribution blocks + shape validation (repo-ready plugin folders, Phase D)",
  "files": {
    "app/tts_server/plugin_manifest.py": {
      "change": "modified",
      "contract_changed": true,
      "summary": "New _validate_distribution() called from _validate_manifest(): optional top-level 'distribution' block must, when present, be a dict with typed fields (host/base_url/repo/git_url/topic strings, pin_ref string-or-null, official bool); no field required. Sanitize-categories body factored into _validate_sanitize_categories() unchanged."
    },
    "tests/tts_server/test_manifest_distribution.py": {
      "change": "added",
      "contract_changed": false,
      "summary": "TDD suite (15 tests): absent-block valid, valid block passes, non-dict/typed-field rejections, and shipped-manifest assertions (distribution matches official_registry repo_url; built_in absent from xtts/voxtral)."
    },
    "tts_engines/tts_xtts/manifest.json": {
      "change": "modified",
      "contract_changed": false,
      "summary": "Added final-shape distribution block: repo audiobook-studio/tts-xtts, git_url matching official_registry, topic audiobook-studio-tts, pin_ref null, official true."
    },
    "tts_engines/tts_voxtral/manifest.json": {
      "change": "modified",
      "contract_changed": false,
      "summary": "Added final-shape distribution block: repo audiobook-studio/tts-voxtral (placeholder GitHub org per problem statement)."
    },
    "design-docs/specs/plugin-contract.md": {
      "change": "modified",
      "contract_changed": true,
      "summary": "v1.7.0: documents the optional shape-validated distribution block; notes standalone repos never set built_in; records voxtral license correction."
    },
    "design-docs/specs/engines-and-plugins.md": {
      "change": "modified",
      "contract_changed": false,
      "summary": "v1.1.3 pointer row to plugin-contract.md v1.7.0 for the distribution block."
    }
  },
  "flow_impact": "Plugin discovery/refresh now rejects a malformed distribution block at manifest-validation time (previously unknown keys were silently ignored). Well-formed and absent blocks behave exactly as before — zero behavior change for shipped plugins. tests/ -k 'manifest or plugin_loader' green (138 passed).",
  "notes": "Validator decision: _validate_manifest does NOT reject unknown top-level keys, so the block would load fine unvalidated — but per the versioned-contracts owner directive the optional-but-shaped validation was added (task 13's optional lenient check, TDD-first). No new required manifest field introduced."
}
