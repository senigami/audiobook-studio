2026-07-02 — W-PAR 006: threaded `active_segments_map` through the full four-point wire (live-jobs.ts OverlayDelta + StudioJobEvent, jobEventAdapters.ts extract, queueOverlayFields.ts whitelist, hydration/index.ts merge) and generalized `useStudioChapter`'s `chapterRenderActiveSegmentsMap`/`chapterRenderRenderingSegmentIds`/`chapterRenderPreparingSegmentIds`/`chapterRenderRenderingBatchProgressById` derivations to drive N simultaneously-active segment bars in the existing ScriptView/ChapterHeader per-span machinery (no ScriptView/ChapterHeader signature changes needed — they already consume Sets/maps generically). Falls back byte-identically to the single `active_segment_id` path when the map is absent (INV-1).

- 2026-07-03 review follow-up: `useStudioChapter.ts` map branches now expand each `active_segments_map` entry to its render-batch siblings (`expandToBatchSpanIds`), restoring the legacy batch-highlight visual contract at cap=1 (INV-1); done-phase entries excluded from the rendering set (reviewer fix). Tests: batch-expansion + done-phase pins in `useStudioChapter.test.tsx`.
