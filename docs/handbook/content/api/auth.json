{
 "title": "Authentication &amp; Rate Limiting",
 "desc": "Securing the gateway for LAN or shared use.",
 "lede": "Before exposing the gateway beyond localhost, lock it down with a key and rate limits.",
 "body": "<div class=\"at-glance\"><div class=\"at-title\">At a glance</div><ul><li>Bearer API-key auth.</li><li>Consider LAN binding carefully.</li><li>Per-IP rate limiting.</li></ul></div>\n        <h2>API key (Bearer)</h2>\n        <p><strong>Authenticate requests with a Bearer token.</strong> Send <code>Authorization: Bearer &lt;key&gt;</code> on each call.</p>\n        <pre><code>curl -H &quot;Authorization: Bearer $STUDIO_API_KEY&quot; \\\n     http://127.0.0.1:8123/api/v1/tts/engines</code></pre>\n        <h2>LAN binding &amp; rate limits</h2>\n        <p><strong>Binding to a LAN address exposes the gateway to your network.</strong> Use the API key, apply per-IP rate limiting, and consider a reverse proxy. See <a href=\"#operations/headless-lan\">Headless &amp; LAN Exposure</a>.</p>"
}