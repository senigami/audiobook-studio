{
 "title": "First Run",
 "desc": "Starting the server and opening the app on port 8123, plus what happens the first time.",
 "lede": "Once installed, starting Studio is a single command. The first launch does a little extra setup, then opens in your browser.",
 "body": "<div class=\"at-glance\"><div class=\"at-title\">At a glance</div><ul><li>Start with the launcher; the app serves on <strong>port 8123</strong>.</li><li>First run downloads engine models and creates working folders.</li><li>Open <code>http://127.0.0.1:8123</code> in your browser.</li></ul></div>\n        <h2>Start the server</h2>\n        <p><strong>Run the launcher and wait for it to come up.</strong> Under the hood it starts the app (<code>uvicorn run:app</code>) and the managed TTS Server. When it's ready, open <code>http://127.0.0.1:8123</code>.</p>\n        <h2>What happens on first run</h2>\n        <ul><li><strong>Model downloads.</strong> The local engine fetches its model the first time it's needed; this can take a while.</li><li><strong>Folders are created.</strong> Studio sets up its projects, voices, and working directories.</li><li><strong>Plugins are discovered.</strong> Installed engines are registered with the TTS Server.</li></ul>\n        <div class=\"note\"><b>Note —</b> First-run model downloads can look like a long pause. Progress is surfaced in the terminal so you can see it's working, not stuck.</div>\n        <h2>Next</h2>\n        <p>Take the <a href=\"#getting-started/quick-tour\">5-Minute Workflow Tour</a> to make your first audio.</p>"
}