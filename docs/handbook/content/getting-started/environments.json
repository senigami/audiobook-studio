{
 "title": "The Two Environments",
 "desc": "Why the local engine lives in a separate Python environment from the web app.",
 "lede": "Studio uses two Python environments. Keeping them apart prevents the app and the heavy engine dependencies from fighting each other.",
 "body": "<div class=\"at-glance\"><div class=\"at-title\">At a glance</div><ul><li><code>./venv</code> — the web app and orchestrator.</li><li><code>~/xtts-env</code> — the heavier local-engine dependencies.</li><li>Separation avoids dependency conflicts.</li></ul></div>\n        <h2>Why two?</h2>\n        <p><strong>The app and the local engine have very different dependencies.</strong> The local cloning engine pulls in large machine-learning packages that can conflict with the app's lighter stack. Isolating them keeps both stable.</p>\n        <h2>What lives where</h2>\n        <ul><li><strong><code>./venv</code></strong> — the FastAPI app, orchestrator, and tooling.</li><li><strong><code>~/xtts-env</code></strong> — the local engine and its model dependencies.</li></ul>\n        <p>The TTS Server (which hosts engines) runs against the engine environment, while the app runs in its own. See <a href=\"#architecture/tts-server\">TTS Server &amp; Watchdog</a>.</p>\n        <div class=\"tip\"><b>Tip —</b> If engine dependencies ever get into a bad state, you can recreate <code>~/xtts-env</code> without touching the app — see <a href=\"#operations/xtts-env\">The XTTS Environment</a>.</div>"
}