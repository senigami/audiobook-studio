{
 "title": "Engines Overview",
 "desc": "Local vs cloud engines, and how Studio routes to them.",
 "lede": "Studio can route synthesis to more than one engine. They split into local and cloud, with one local engine installed by default.",
 "body": "<div class=\"at-glance\"><div class=\"at-title\">At a glance</div><ul><li><strong>Local engines</strong> run on your machine — XTTS is the only default install.</li><li><strong>Cloud engines</strong> are optional and need an API key (e.g. Voxtral).</li><li><strong>Composite</strong> synthesis mixes engines within a chapter.</li><li>More engines can be added as plugins.</li></ul></div>\n        <h2>Local engines</h2>\n        <p><strong>The default engine is local and private.</strong> XTTS is the one engine installed out of the box; it runs on your machine with no account. See <a href=\"#engines/xtts\">Local Engine (XTTS Default)</a>.</p>\n        <h2>Cloud engines</h2>\n        <p><strong>Cloud engines are optional and opt-in.</strong> You enable one with an API key; none ship by default. Voxtral is one example. See <a href=\"#engines/voxtral\">Cloud Engines</a>.</p>\n        <h2>Composite synthesis</h2>\n        <p><strong>A chapter can mix engines.</strong> Composite synthesis stitches output from multiple engines/voices into one chapter. See <a href=\"#engines/composite\">Composite Synthesis</a>.</p>\n        <h2>Plugins add more</h2>\n        <p><strong>Engines are installable.</strong> Beyond the default, you can add engines as plugins from GitHub. See the <a href=\"#plugin-sdk/overview\">Plugin SDK</a>.</p>"
}