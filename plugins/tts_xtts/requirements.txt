# XTTS Local Requirements
# These are the base packages required to run the XTTS adapter.
requests>=2.31.0
pydantic>=2.0.0

# Inference Environment Dependencies
# Using the Idiap community fork for maintenance (2026)
git+https://github.com/idiap/coqui-ai-TTS.git@main

# Specific dependencies for compatibility with the Idiap fork
coqpit-config==0.2.4
transformers<5.0.0
torch>=2.10.0
torchcodec
torchvision
torchaudio
